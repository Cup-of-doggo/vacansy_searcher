[project]
name = "vacansy_searcher"
version = "0.1.0"
description = ""
authors = [
    {name = "Gordey",email = "gordej.taran@mail.ru"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest (>=8.3.4,<9.0.0)",
    "openpyxl (>=3.1.5,<4.0.0)",
    "flake8 (>=7.1.2,<8.0.0)",
    "pytest-cov (>=6.0.0,<7.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
